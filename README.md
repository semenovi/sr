# sr
Usage:

    sr.exe
    -m mode         interpolation, index
    -e method       nearest, bilinear - for interpolation
                    psnr, ssim - for index
    -i image        path
    -s scale        only for interpolation
